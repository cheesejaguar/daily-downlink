# Local preview only. GitHub Pages does NOT read this file -- it builds with its
# own pinned toolchain. This exists so `bundle exec jekyll build` locally uses
# exactly what Pages uses, so a clean local build means a clean deploy.
#
#   github-pages 232  ==  Jekyll 3.10.0 on Ruby 3.3.x
#   (authoritative list: https://pages.github.com/versions.json)

source "https://rubygems.org"

gem "github-pages", "~> 232", group: :jekyll_plugins
